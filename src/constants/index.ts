export const LOGGED_IN_COOKIE = 'DEMO_SCREEN_LOGGED_IN'

export enum ROLE {
  SUPER_ADMIN = 'superAdmin',
  ADMIN = 'admin',
}

export enum RealtimeDB {
  ALLOWED_USER = 'allowedUsers',
}

export enum COLLECTION {
  USERS = 'users',
}
